# Строгост: без достъп до пакети, които не са обявени изрично като зависимост.
# Точно това предпазва от „работи при мен, пада на сървъра".
strict-peer-dependencies=false
auto-install-peers=true
resolution-mode=highest
engine-strict=true
