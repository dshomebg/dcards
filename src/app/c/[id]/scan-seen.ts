// Общото между `/c` (слага) и `/{slug}` (чете): името и живота на маркера.

export const SCAN_SEEN_COOKIE = 'scan_seen';

/** Само за прехода чип → профил; повторно отваряне след това е „линк". */
export const SCAN_SEEN_SECONDS = 30;
