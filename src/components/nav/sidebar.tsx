/**
 * Страничният панел — обвивката на навигацията при ШИРОК екран.
 *
 * Панелът е СВЕТЪЛ и се отделя от съдържанието с граница, не с цвят. Тъмен
 * панел върху светло съдържание е точно контрастът, който собственикът отхвърли.
 *
 * Под прага изчезва напълно и се вика от бутона в лентата (`NavDrawer`): при
 * 390 пиксела ширина той изяждаше 256 от тях и полетата ставаха по-тесни от
 * подсказките под тях (`ADM-9` § 2).
 */

import { NavMenu } from './nav-menu';

export function Sidebar() {
  return (
    <NavMenu
      className={
        // Ширината идва от двете колони, не се задава тук: лентата е 24, а
        // колоната 56 и свиването я маха съвсем. Превъртането е в самата
        // колона — там е дългият списък.
        'hidden shrink-0 wide:flex'
      }
    />
  );
}
